const app = require('../server/app')
// const config = require('../server/config/env')

// Start server
app.listen(3900, function () {
  console.log('Koa server listening on %d, in %s mode', 3900)
})
